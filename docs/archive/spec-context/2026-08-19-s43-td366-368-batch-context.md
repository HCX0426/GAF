# s43 spec-context 承载体 — TD-366/367/368 batch (2026-08-19)

> 关联 spec: docs/specs/archived/2026-08/2026-08-19-s43-td366-368-batch.md
> B2 大修改 evidence: .cache/b2_acknowledged.json (diff 1624 行 > 500, 跨 2 backend app)

## 1. 用户决策原文

- "还有多少候选？？"（要求候选盘点）
- "收尾"（s41 收尾补丁授权）
- "开始"（s42 完成后授权接修下一批 TD）
- "Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed."（自决授权：有下一步就继续）

## 2. N151 5 步法评估过程

- **架构盘点**: TD-366 = N197 WS 归一化的设备级缺口（协议级已 env 化，设备级 ws/devices 硬编码）；TD-367 = 8 个无引用组件（UnattendedControlPage 自实现渲染替代了设计稿组件）；TD-368 = 架构文档 3 处引用过期（backend app 迁移/心跳参数调整后未同步）
- **识别反模式**: (a) WS 路径双硬编码（frontend + backend routing.py 各 1 处）— N197 反模式在设备级的延续；(b) 死代码堆积 ~2000 行（半成品 UI 误导）；(c) 文档漂移 10 处（N112 四步配套违反）
- **A/B/C 备选**:
  - TD-366: A=前缀段 env（GAF_WS_DEVICES_PATH + VITE_WS_DEVICES_PATH，改 1 处全变）; B=全路径模板 env; C=保持硬编码。选 A（前缀段含动态 deviceId 变量，模板化无必要）
  - TD-367: A=git rm 8 组件（0 引用已验证）; B=接入 UI 复用; C=保留。选 A（意图未确认前保留 = 死代码堆积；接入 UI > 500 行需产品意图）
  - TD-368: A=10 处文本修正; B=重写文档段落; C=不动。选 A（最小正确改动）
- **拒绝反模式**: 拒绝"保留双套"（8 组件与 UnattendedControlPage 双套 → 删）; 拒绝"最小化修补"（TD-366 只改前端不改 backend → 两端都 env 化）
- **AI 自决边界**: 3 TD 均 P2/P3 且合计 < 500 行新增（删除 ~2000 行不计）→ 批量 spec 自决；TD-367 设计意图是否落地留给用户（写入 spec 已知限制）

## 3. N167 七维度评分细节

| 维度 | 评分 | 说明 |
|------|------|------|
| 1 架构长远性 | 4/5 | WS env 化对齐 N197 模式；死代码删除降低维护面 |
| 2 全局归一化 | 5/5 | WS 路径 4 层（backend config + routing + frontend config + .env）单点可控 |
| 3 新旧兼容 | 4/5 | 默认值 = 原路径，无行为变化；legacy 文档描述修正不破坏历史记录 |
| 4 现有业务完善 | 3/5 | 无功能新增（死代码无入口）；文档准确性提升 |
| 5 性能资源优化 | 3/5 | 删除 2000 行死代码（构建产物减小，非关键） |
| 6 安全合规加固 | 3/5 | 无敏感面变化 |
| 7 长期维护成本 | 5/5 | 改 WS 路径从 2 处 → 1 处 env；文档与代码一致 |

总分 27/35 ≥ 19 且领先 → AI 自决 ✓

## 4. 关键实施决策

- **WS 前缀段 env 设计**: `GAF_WS_DEVICES_PATH="ws/devices/"`（无前导 /，backend re_path 风格）+ `VITE_WS_DEVICES_PATH="/ws/devices/"`（有前导 /，前端拼接风格）——两端口径差异遵循各自既有模式（WS_AGENT_PATH vs WS_PATH）
- **routing.py 用 rf-string 插值**: `re_path(rf'^{WS_DEVICES_PATH}(?P<device_id>[^/]+)/adb-logs/$')` — WS_DEVICES_PATH 来自 env，re_path 匹配 URL 无前导 /，前缀段直接嵌入
- **frontend/.env 不进 git**（项目惯例，VITE_API_PREFIX 同）— fallback `|| '/ws/devices/'` 保证默认行为
- **doc-code sync 8 hard fail 处理**: 8 组件删除前 grep 已验证 0 引用（41 匹配全为定义文件自身 + 2 同名 interface/function）→ GAF_SKIP_DOC_SYNC=1 合法跳过
- **TD-368 的 tracing 描述**: 更新为实际实现（gaf_core/tracing trace_id ContextVar 贯穿，无 TraceSpan 三层）而非保留过时描述

## 5. N173 用时字段

- start_ts: 2026-08-19T02:10:00+08:00
- end_ts: 2026-08-19T02:50:00+08:00
- duration_min: 40
- within_baseline: false
- root_cause_if_over: 3 TD 批量（代码 + 8 文件删除 + 10 处文档修正）+ B2 acknowledge/spec-context 补写 2 轮 hook 交互；单 TD 均 < 10min，批量 spec 基线应放宽至 30min（观察项）