# s43: TD-366/367/368 批量修复 — WS 路径 env 化 + 死代码清理 + 文档漂移修正

> 状态: ✅ 已归档 (commit -) | spec_id: 2026-08-19-s43-td366-368-batch
> 来源: L3-1 扫描登记 TD（s42）→ TD 处理顺序接修（P2 优先）
> 创建: 2026-08-19 | 基线: N173 中修改 < 15min
> 归档: docs/specs/archived/2026-08/
> 实现产物: WS env 化 4 文件 + 8 组件删除 + 10 处文档修正 + TD-366/367/368 迁移 fixed

## 阶段状态表

| 阶段 | 状态 | 完成时间 | commit | 验收 evidence |
|------|------|---------|--------|--------------|
| P1 TD-366 WS 路径 env 化 | ✅ | 2026-08-19 | - | 见 evidence |
| P2 TD-367 死代码删除 | ✅ | 2026-08-19 | - | 见 evidence |
| P3 TD-368 文档漂移修正 | ✅ | 2026-08-19 | - | 见 evidence |
| P4 验证 + 迁移 + 归档 | ✅ | 2026-08-19 | - | 见 evidence |

## 背景

L3-1 扫描（s42）登记 TD-366/367/368。按 §4.8 TD 处理顺序批量接修（P2+P2+P3，合计 < 500 行代码 + ~2000 行删除）。

## 任务清单

### P1: TD-366 — AdbLogViewer WS 路径 env 化（N197 覆盖）

**事实**：`frontend/src/pages/Devices/AdbLogViewerPage.tsx:127` 硬编码 `/ws/devices/${id}/adb-logs/`；backend `agents/routing.py:11` 硬编码 `ws/devices/...`。

- [ ] 1. `backend/config/app_info.py`: 加 `WS_DEVICES_PATH = os.getenv("GAF_WS_DEVICES_PATH", "ws/devices/")`（风格同 WS_AGENT_PATH）
- [ ] 2. `backend/agents/routing.py`: `re_path(rf'^{WS_DEVICES_PATH}(?P<device_id>[^/]+)/adb-logs/$', ...)` + import WS_DEVICES_PATH
- [ ] 3. `frontend/src/config/app.ts`: `export const WS_DEVICES_PATH = import.meta.env.VITE_WS_DEVICES_PATH || '/ws/devices/';`
- [ ] 4. `frontend/.env`: `VITE_WS_DEVICES_PATH=/ws/devices/`
- [ ] 5. `AdbLogViewerPage.tsx`: url 拼 `${WS_DEVICES_PATH}${selectedDeviceId}/adb-logs/` + import

### P2: TD-367 — 8 个死组件删除

**已验证 0 引用**（grep: 41 匹配全为定义文件自身 + 2 个同名 interface/function）：TimeWindowConfig / SwitchIntervalConfig / ExecutionPlanPreview / DeviceWarmupEditor / ConcurrencyMatrixPanel / AutoStopConditions / AccountRotationEditor / NodePreviewModal

- [ ] 6. 删除 8 个组件文件（`git rm`）；NodePreviewModal 在 components/Pipeline/

### P3: TD-368 — 文档漂移修正（10 处）

- [ ] 7. optimal-solution.md:349 `backend/agent/handlers/verify.py` → `backend/device_bridge/handlers/verify.py`
- [ ] 8. optimal-solution.md:145 `engine/graph.py` 状态 🔧(无独立graph.py) → ✅ 存在
- [ ] 9. overview.md:537 删 devices/ 顶层 worker_pool.py 行（实际 adb/pool.py，L532 已覆盖）
- [ ] 10. overview.md:558 `core/chain.py` → `core/chain_manager.py`
- [ ] 11. features-overview.md:50 心跳 15s/30s → 10s/30s
- [ ] 12. features-overview.md:280 6 模块 app 去 tracing/metrics → 4 个
- [ ] 13. features-overview.md:289 tracing/spans → gaf_core.tracing (trace_id)
- [ ] 14. features-overview.md:351-354 metrics/sla → monitors/sla (SLAMetricViewSet, monitors/urls.py:19)
- [ ] 15. features-overview.md:474 去 i18n/（并入 gaf_core）
- [ ] 16. features-overview.md:564-566 i18n//tracing//metrics/ → gaf_core.i18n / gaf_core.tracing / monitors

### P4: 验证 + 迁移 + 归档

- [ ] 17. 验证：backend routing 测试（protocol/tests）+ 前端 vite build + grep 无 `ws/devices` 硬编码 + ruff
- [ ] 18. evidence 三件套 + TD-366/367/368 迁移 active → fixed
- [ ] 19. 归档 spec

## 已知限制

- TD-367 组件设计意图（执行计划预览等）是否需落地由用户后续决策，本次仅清理死代码

## Deviation Log

- （无）